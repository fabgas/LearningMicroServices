docker build -t "book/mircoservice" ../mircoservices
docker build -t "book/gamification" ../gamification
docker build -t "book/gateway" ../gateway
docker build -t "book/service-registry" ../service-registry
